# AI-Office ROADMAP

Живой план реализации мульти-агентной системы для производства лендингов под клиентов кастдева. Источник истины по архитектуре офиса.

## Архитектура

### Pipeline (sequential, файловый handoff)
транскрипт кастдева (с диаризацией)
→ client-strategist     [что клиент хочет — по реакции, взвешенно]
→ product-architect     [фильтр реализуемости + реальные цифры, 2 выхода]
→ pricing-strategist    [цена от сегмента + себестоимость, оффер, скидка за кейс, 2 выхода]
→ dr-copywriter         [продаёт хотелки в рамках реального; читает spec_brief + pricing_offer_brief]
→ landing-designer      [дизайн из копи]
→ frontend-dev          [код + деплой (сам, через skill deploy_landing)]
Гейты перед человеком (не линейный шаг — проверяют готовые артефакты, независимые оси):
  • qa-controller    [честность: фабрикация / slop / соответствие брифу — «можно ли показывать»]
  • sales-evaluator  [продающая сила — «продаёт ли»]
Директор — оркеструет всю цепочку, ведёт диагностику. Обязателен для боевой работы.

### Модель автономности

ЦЕЛЬ: полная автономность. Директор сам решает что в его компетенции, к Алику идёт только когда сам не может (редко). На выходе — готовый результат + диагностический отчёт.

**Два РЕЖИМА** (не gates на каждом шаге — это уровень автономности одного и того же Директора, настройка, не переписывание):
- **РЕЖИМ ОТЛАДКИ (сейчас):** детальное логирование каждого прогона. Алик смотрит как каждый агент отрабатывает, может вмешаться. Цель — убедиться что агенты работают корректно.
- **РЕЖИМ ПРОДАКШН (цель):** полностью автономный прогон. На выходе — готовый лендинг + отчёт. Эскалация к Алику только в крайних случаях.

**Этапы развития:**
- **ЭТАП 1:** автономный прогон цепочки + детальные логи + диагностический отчёт. Агенты передают работу друг другу поэтапно (файловый handoff, как живые сотрудники). Директор следит что цепочка идёт корректно.
- **ЭТАП 2:** Директор включается в процесс, когда у агентов есть предложения/вопросы — подзывает нужных агентов, они решают совместно, результат возвращается в pipeline («поднятие руки»).
- **ПОТОМ:** оценить эффективность на практике.

**Эскалация к Алику — техническое ограничение.** Эскалация = Директор ОСТАНАВЛИВАЕТСЯ и возвращает «решил всё кроме X, нужно решение Алика», Алик отвечает, Директор продолжает с этой точки. Это НЕ «живой звонок посреди прогона» (субагенты неинтерактивны). Для редких случаев нормально. В отчёте видно где и почему остановился.

### Принципы
- Каждый агент ДОВЕРЯЕТ предыдущему и углубляет, НЕ перепроверяет с нуля. Не дублировать роли → видно какой этап проседает.
- Источник истины — транскрипт с диаризацией. Производные файлы (старые meta/structure/package по PDF-логике) НЕ давать агентам — искажают. Только стратег читает транскрипт, остальные доверяют артефакту предыдущего.
- Узнавание, не пересказ: клиент должен УЗНАВАТЬ свою картину ("да, вот это я искал"), а не слышать пересказ слов. Не цитировать в лоб.
- Баланс: резонировать с миром клиента + держать планку качества презентации. Не гладкий корпоративный маркетинг, но и не сырые цитаты.
- Взвешивание реакции клиента (стратег): три уровня (высокий/средний/низкий), не бинарно. При сомнении — перечитать транскрипт глубже. Калибровать по личности клиента.
- Честность + эскалация: не знаешь → ищи в интернете (где разрешено) → не решил → Директор → не решил → Алик. Не замалчивать, не придумывать.
- Модели: сейчас **ВСЁ на opus** (качество — приоритет на этапе отладки). Все агенты — стратег, архитектор, копирайтер, дизайнер, frontend-dev, qa-controller, Директор — на opus.
- ОПТИМИЗАЦИЯ (после отладки, если opus много потребляет): кандидаты на понижение до sonnet — **frontend-dev** (собирает HTML по готовой спеке = механика) и **qa-controller** (проверка по чеклисту = механика). Понижать по факту, если качество не падает. Думающие роли (стратег, архитектор, копирайтер, дизайнер) остаются opus.

## Агенты

### client-strategist ✅ собран, отлажен
Вход: транскрипт. Выход: strategist_brief.md.
Анализирует что клиент хочет по реакции (взвешивание), различает спикеров, формирует разделы: аватар / awareness (Schwartz) / оффер / механизм / VoC-цитаты / "Стоп-лист: не использовать" / "Слабые сигналы на будущее".
Tools: Read, Write, Glob, Grep, Skill.

### product-architect — ПРОАКТИВНЫЙ продуктовый архитектор (КРИТИЧНОЕ звено) 🛠 усилен v2 (не тестирован после усиления)
**Усиление v2 (2026-06-01):** из реактивного «фильтра реализуемости» → в ПРОАКТИВНОГО продуктового архитектора, который САМ исследует рынок и приходит с готовым предложением продукта. Добавлено: (1) проактивное предложение (не «ответь на вопросы», а «вот продукт»); (2) ОБЯЗАТЕЛЬНЫЙ ресёрч рынка перед предложением (аналоги, инструменты топовых AI-разработчиков, паттерны интеграции — с выводами под клиента); (3) инструменты с вариантами + рекомендация; (4) предметное описание (как работает шаг за шагом, не лозунгом); (5) варианты продукта на вывод; (6) **градация по пользе — закрывает ДЫРУ 2**; (7) ДВА РЕЖИМА (АВТОНОМ / ИНТЕРАКТИВНЫЙ — точка согласования продукта с Аликом). Честность-дисциплина (3 уровня, sanity-check цифр) сохранена как хребет, не ослаблена проактивностью.
Проблема которую решает: pipeline продаёт желания, не проверяя реализуемо ли (сочинённые цифры "за 5 минут" недопустимы) + кто-то должен ПРИДУМАТЬ из желаний сильный современный продукт со знанием рынка, чтобы Алик понимал, что предлагает.
Роль: ДАЁТ копирайтеру что нужно (реальное + цифры + возможности), копирайтер сам решает что использовать. НЕ режет за копирайтера, НЕ грузит техническими деталями. Может ПРЕДЛАГАТЬ (предложения идут обратно стратегу — в Этапе 2 через Директора; в Этапе 1 пишет в заметки, Алик читает).
Оценивает через призму Claude Code + доступных инструментов — НЕ ограничен навыками Алика (с Claude Code возможно многое).
Вход: strategist_brief.md.
ДВА выхода (оба сразу):
  - product_spec_brief.md — для копирайтера. Сжатая рамка реальности: что реально + цифры, что не обещать. Без технических потрохов.
  - product_plan.md — для Алика. Подробный план реализации: что, как, стек, этапы, готовый роадмап который останется запустить в новом проекте для клиента.
Честность: знает → оценка; не уверен → ищет в интернете, проверяет (API, цены, лимиты); не решил → эскалация. Цифры обоснованы или честное "надо измерить/прототип". НЕ сочинять.
Финальная проверка реализуемости — за Аликом.
Tools: Read, Write, Skill, WebSearch, WebFetch (интернет для проверки фактов).
Модель: **opus** (оценка реализуемости + проверка фактов = сложное рассуждение; решено на сборке).

### pricing-strategist — ценовой стратег 🛠 собран (черновик v2, не тестирован)
Стоит между архитектором (что построили + себестоимость) и копирайтером (оффер/CTA). Закрывает дыру оффер/CTA через обоснованную цену, а не выдуманную.
Вход: `product_plan.md` (состав, сложность=часы, себестоимость API verified; + что построено vs вижн) + `strategist_brief.md` (отношение к деньгам, психология, стоп-лист).
Метод (6 шагов): (1) **ресёрч по СЕГМЕНТУ** (частник/соло, НЕ enterprise — тот лишь верхний референс) + **триангуляция нового жанра** (личная бизнес-машина: кастомная разработка / абонентки на агентов / ретейнеры эксперта; явно проговорить разрыв, не натягивать ложный комп); (2) **себестоимость снизу** — ставку часа ВЫВОДИТ сам (оклад-ориентир + senior-ставки РФ через WebSearch → диапазон → Алику на подтверждение; якорь ~4000 ₽/ч, пол первого кейса ~1800, два пола не путать); (3) **потолок** + **гард против недооценки** (право спорить с Аликом вниз по цене: «дёшево=подозрительно» → не поддакивать полу); (4) **структура оффера** — уклон в **фикс-абонентку** (инфра+поддержка+маржа), pass-through исключение; (5) **цена + скидка за кейс** — честный обмен (кейс/отзыв/терпимость), скидка **только на созвоне**; (6) **переговоры** — цена на лендинг по умолчанию НЕТ (искл. продуктизированный фикс-оффер), якорь, формат следующего шага в копи.
ДВА выхода: `pricing_offer_brief.md` (копирайтеру — формат/CTA, без экономики и цены) + `pricing_strategy.md` (Алику — полная стратегия с расчётами).
Честность: рыночное через WebSearch (свежие данные, не память — урок про цену Opus), ни одной вилки из воздуха, финал по цене за Аликом.
Два режима: АВТОНОМ / ИНТЕРАКТИВНЫЙ (вся работа автономно → одна поздняя стоп-точка на числе; за тем что может исследовать сам — к Алику не ходит).
Tools: Read, Write, Skill, WebSearch, WebFetch. Модель: opus.

### dr-copywriter ✅ собран (НЕ тестирован) — теперь читает ДВА брифа
Вход: strategist_brief.md + product_spec_brief.md (рамка реального от архитектора) **+ pricing_offer_brief.md (оффер/цена/формат — главнее по цене/CTA)**.
Продаёт хотелки клиента в пределах реального, оформляет продающе (копирайтер = и маркетолог). Оркеструет skills: headline-matrix, objection-crusher, conversion-path-builder, copywriting → чистка generic-language-killer → stop-slop (обязательный gate ≥35/50) → humanizer.
**stop-slop остаётся встроенным в копирайтера** (обязательный gate ≥35/50). qa-controller — дополнительный слой контроля поверх, НЕ замена stop-slop: копирайтер сам чистит свой текст, QA проверяет результат всего pipeline.
Выход: copywriter_draft.md. Доверяет брифу, в транскрипт не лезет.
Tools: Read, Write, Edit, Skill.
Модель: opus.

### landing-designer 🛠 собран (черновик, не тестирован)
Вход: copywriter_draft.md. Паттерн design-bridge + skill frontend-design (первым). Выход: design_spec.md. Anti-convergence (каждый лендинг уникален). Cialdini-триггеры. Модель opus.

### frontend-dev 🛠 собран (черновик, не тестирован)
Вход: design_spec.md + copywriter_draft.md (точные слова). Узко под статический HTML, без мультифреймворк-раздувания. Lighthouse ≥95, accessibility, SEO. Выход: код в deliverables/<slug>/.
**Деплой — САМ** (не Директор): после готового кода вызывает skill `deploy_landing` (автодеплой на Vercel) → live URL. Fallback: если автодеплой заблокируется (классификатор на outward-facing действие) — готовит всё к деплою и сообщает Алику точную команду для ручного запуска. НЕ блокер — либо авто, либо руками, процесс не встаёт.
Модель: opus (кандидат на понижение до sonnet после отладки — сборка HTML по готовой спеке = механика).

### qa-controller — гейт честности/качества 🛠 собран (черновик, не тестирован)
**ОТДЕЛЬНЫЙ агент** (решено — не скилл Директора). Обоснование: чистое разделение ролей (в отчёте Директора видно отдельную строку QA), изоляция для диагностики (видно, проседает ли контроль качества как этап), гибкость вызова на разных точках.
**Роль:** гейт ПЕРЕД человеком. Проверяет финальные клиентские артефакты (копи/лендинг/доки) по **трём осям**: (1) **фабрикация** — факт/метрика/имя/цитата без опоры на транскрипт; directional как verified; непостроенное (память/ров) как готовое; утечка имени в `<head>`; абсолюты-мины (хребет честности + уроки «verified≠правда», «Альфа Холдинга», caveat памяти, приватность имени); (2) **slop** — дженерик-вода/канцелярит/AI-tells; (3) **соответствие** — не теряет/не искажает бриф предыдущего агента + стоп-лист + границы. **НЕ оценивает силу продажи** (это sales-evaluator — ортогональная ось).
**Поведение:** сам НЕ правит → вердикт PASS/BLOCK + отчёт (`_qa_report.md`: что/где/тип/опора/фикс) + эскалация по офисной схеме (`_agent_notes.md` → производящий агент / Директор / Алик). Фабрикация = жёсткий блок.
**Исключение:** ЧИТАЕТ транскрипт (как верификатор — иначе грунтовку не проверить). Дисциплина против ложных срабатываний: авторский парафраз / «для примера» / directional-как-directional — НЕ нарушения.
**Режимы:** финальный гейт (default, рядом с sales-evaluator) / сквозной handoff-контроль (опц., не дефолт — ловить искажение рано).
Кирпичи: stop-slop (слой ПОВЕРХ встроенного в копирайтера, не замена), generic-language-killer как инструмент.
Tools: Read, Write, Edit, Grep, Glob, Skill, WebFetch.
Модель: opus (сейчас; кандидат на понижение до sonnet после отладки — проверка по чеклисту = механика).

### sales-evaluator — оценщик продаж ⬜ [ПРИОРИТЕТ, собрать первым]
Роль: эксперт по продажам, оценивает результат со стороны «продаёт ли». **НЕ путать с qa-controller** (тот про факты/slop/соответствие брифу) — оценщик про продающую СИЛУ.
Оценивает: захват внимания, ясность оффера, **показан ли результат предметно**, работа с возражениями, CTA, доверие, структура убеждения, вау-фактор.
Две точки вызова: **копи** (текстовый режим, критерии 1–8 /80, до дизайна — поймать слабое рано) + **финал** (визуальный режим, критерии 9–12 /40, по рендеру готового лендинга).
**Визуальный проход (V) ✅ инфра закрыта (2026-06-06).** Был «потерян» в оркестрации: режим 9–12 в `sales-evaluator.md` существовал, но Директор звал оценщика только на копи (до сборки) и PNG не рендерил → визуальные критерии не срабатывали. Закрыто: (1) `scripts/render_screenshot.py` — честный fullPage-захват (playwright + системный chromium, скролл всей высоты + `reduced_motion` → без фантомных пустот; рецепт из `failures.md` #screenshot #visual-eval и `benchmark_coach_vs_ours.md`); (2) в Директор вшит пост-фронтовый шаг **V**: рендер `deliverables/<slug>/` → PNG → `sales-evaluator` визуальным режимом. **Информативно, не авто-блок**; эскалация Алику С PNG (решает глазами); назад к дизайнеру НЕ маршрутизируем (Этап 2). Отдельно от G2-честности. Запуск: `/usr/bin/python scripts/render_screenshot.py deliverables/<slug>/ -o /tmp/shots/<slug>.png` (playwright стоит под `/usr/bin/python`).
Выход: обратная связь (что сильно / что слабо / что усилить) + **СКОР по критериям** (для сравнения версий между собой).
При нескольких вариантах — **РАНЖИРУЕТ** (какой сильнее и по чему).
Кирпичи: performance-diagnosis, conversion-path-builder, marketing-psychology.
Модель: opus.

### Директор — оркестратор 🛠 собран (черновик, не тестирован), обязателен для боевой работы
Директор = главный агент-оркестратор. Цель — **автономность**. Запускает весь pipeline по команде "собери лендинг для <клиент>".
- Оркеструет цепочку (файловый handoff, агенты передают работу поэтапно — как живые сотрудники).
- **РЕШАЕТ САМ** что в его компетенции (на то и директор — не дёргает Алика по мелочам).
- **Эскалирует Алику только когда сам не может** (остановка + возврат «решил всё кроме X», редко).
- **ЛОГИРУЕТ каждый прогон детально** (что каждый агент делал — для разбора).
- **ДИАГНОСТИЧЕСКИЙ ОТЧЁТ** (_director_report.md): что делали агенты; были ли общения между ними и что решали; какие решения Директор принял БЕЗ Алика; эскалации (где не смог сам). Алик читает отчёт постфактум, в логи заглядывает если нужно больше деталей.
- Технический паттерн оркестрации — взять из orchestration-research.md (agent-organizer, multi-agent-coordinator, "When invoked / Integration with other agents").
- Модель: opus.

(Раньше тут были «4 human gates на каждом этапе» — УБРАНО, это не модель Алика. Вместо gates — автономность + отчёт + редкая эскалация. См. раздел «Модель автономности».)

Деплой — НЕ обязанность Директора. Директор оркеструет, **деплоит сам frontend-dev** (через skill `deploy_landing`). Директором деплой не грузить.

### Логирование (RUN ID + timestamp, ВСЕГДА)
Логи ведутся **всегда** (не только в режиме отладки) — для оптимизации и трассируемости.
- Каждый прогон получает **RUN ID** (уникальный номер прогона). Директор присваивает его в начале прогона и **прокидывает агентам** (вместе с датой) — они логируют с ним.
- Каждое действие агента логируется с **timestamp (время) + RUN ID**.
- Лог пишется в `clients/<slug>/_run_logs/run_<id>_<date>.log`.
- Зачем: сравнивать прогоны между собой («в прогоне №3 стратег дал X, в №5 — Y»), видеть тренды где регулярно проседает, синхронизироваться по номеру + времени.
- Лог (сырой, по действиям) ≠ `_director_report.md` (свод/диагностика). Отчёт — для чтения Аликом постфактум, лог — для глубокого разбора и сравнения прогонов.

## Голос сотрудника (_agent_notes.md)
Файл ОБЩИЙ на клиента (clients/<slug>/_agent_notes.md), в папке клиента → пометка клиента не нужна.
Формат: "## КТО → КОМУ · дата" + замечание/предложение + важность.
ЭТАП 1: агенты пишут, Алик читает и дописывает решения вручную ("## Решение (Алик)").
ЭТАП 2: Директор читает, маршрутизирует, ведёт решения сам.
Правило: писать только когда реально есть что сказать (не спамить).

## Статус задач

### Сделано
- [x] Этап 0: 19 skills установлены, закоммичены (2420a82), запушены
- [x] client-strategist — собран, отлажен (взвешивание реакции, источник=транскрипт, стоп-лист+слабые сигналы)
- [x] dr-copywriter — собран, роли разделены (НЕ тестирован)
- [x] Исследование оркестраторов (zubair + VoltAgent) → orchestration-research.md
- [x] Решения по архитектуре офиса зафиксированы (этот роадмап)
- [x] Ответы на ночные вопросы (`_night_questions.md`) применены: #2 product-architect → **opus**; #3 дата прокидывается Директором (воркеры не оставляют `<дата>`-плейсхолдер); #5 две достройки ратифицированы — **dr-copywriter ← product_spec_brief** (опц. рамка реального) и **frontend-dev ← copywriter_draft** (точные слова). #1 (запуск Директора) → см. п.7 порядка сборки. #4 (дубль скиллов) → разбор отдельно (это симлинки, не дубль — см. ниже).

### Порядок сборки (полноценный офис)
- [x] 1. Тест dr-copywriter на брифе Никиты — прогнан (v1 без рамки, v2 с product_spec_brief). Ядро работает.
- [x] 2. Два раздела в стратеге: "Стоп-лист" + "Слабые сигналы на будущее" (ночная сборка)
- [x] 3. Механизм _agent_notes.md в стратега и копирайтера (ночная сборка)
- [x] 4. product-architect (5-й агент, 2 выхода) — собран И протестирован на Никите (поймал косяк с ценой Opus → исправлен + правило sanity-check цифр)
- [x] 5. landing-designer — собран И протестирован на Никите (концепция «Чертёж системы», frontend-design первым, не generic)
- [x] 6. frontend-dev — собран И протестирован на Никите (лендинг собран, задеплоен → arhiv-sistemy.vercel.app)
- [x] 6.5. qa-controller (гейт честности/качества) — собран черновиком (отдельный агент, не скилл Директора). 3 оси: фабрикация / slop / соответствие. НЕ тестирован. Парный гейт к sales-evaluator (ортогональные оси).
- [~] 7. Директор — ПЕРЕОСМЫСЛИТЬ под автономность (собранный черновик заточен под gates). Режим отладки = детальные логи + возможность вмешаться (Вариант А: роль в основной сессии). Режим продакшн = автономный Task-оркестратор + отчёт + редкая эскалация. Один Директор, разный уровень автономности.
- [ ] 8. ПИЛОТ: Никита через весь офис end-to-end
- [ ] 9. Архивация старого PDF-пайплайна → _archive/ (structure_prompt, package_prompt, generate_pdf, старые правила CLAUDE.md 1-5, старые выжимки клиентов). Оставить: kickoff, transcribe Deepgram, fill_meta, COMPASS, failures, транскрипты
- [ ] 10. ЭТАП 2: «поднятие руки» — Директор подзывает агентов когда у них есть предложения/вопросы, решают совместно, результат в pipeline (после рабочей первой версии)

### Этап 1 пройден — что дальше (см. раздел «Расширение офиса»)
Все 5 звеньев прогнаны по отдельности end-to-end (стратег → архитектор → копирайтер → дизайнер → фронтендер → live). Обратная связь Алика на первый лендинг выявила дыры → расширяем офис. Новый порядок:
- [ ] A. **sales-evaluator** (оценщик продаж) — ПРИОРИТЕТ, собрать первым + прогнать на лендинге Никиты (диагноз «почему не вау»)
- [ ] B. Усиления по фидбеку оценщика: копирайтер (предметный показ результата + вау без потери честности), дизайнер (визуализация результата), архитектор (градация решений по пользе)
- [ ] C. Версионирование лендингов (`_sites_registry.md` + skill `manage_sites`)
- [ ] D. Технический лендинг для Алика (карта возможностей) — отложено
- [ ] E. Директор (оркестрация всего) — см. п.7

## Расширение офиса (после Этапа 1)

Источник — обратная связь Алика на первый собранный лендинг (Никита). Принцип: усиливать существующих агентов, не плодить новых без нужды (исключение — оценщик, это новая роль).

### Выявленные дыры (обратная связь Алика на первый лендинг)

**ДЫРА 1 — лендинг «суховат», не показан результат предметно.**
Клиент не видит ЧТО получит и КАК это работает. Нет визуализации результата. Перестраховались со сдержанностью (Никита — скептик) → сместить баланс к «покажи и вдохнови», не теряя честности.
Решение (усилить существующих):
- **Копирайтер:** больше предметного показа результата («вот что ты увидишь» вместо «обещаем фичу»), больше вау — не теряя честности.
- **Дизайнер:** визуализация результата — схемы «как работает», наброски экранов (сводка, регламент), до/после. Презентабельно, НЕ буквально (принцип понятен и вдохновляет, не пиксель-точный макет).

**ДЫРА 2 — нет технической карты возможностей для Алика. ✅ ЗАКРЫВАЕТСЯ (усиление архитектора v2, 2026-06-01).**
Архитектор даёт `product_plan`, но нужна СВОДКА: всё, что можно реализовать из запроса клиента, с **градацией по пользе** (уровни: чем вариант сложнее/полезнее — тем выше уровень; напр. AI-ассистент > документ-машина по пользе). Архитектор достаёт МАКСИМУМ возможного из транскрипта, не только то, что пойдёт в лендинг.
**Сделано:** (1) промпт `product-architect.md` усилен — раздел «Градация по пользе» теперь обязательный, + проактивное предложение, ресёрч рынка, предметное описание, инструменты с вариантами, 2 режима. (2) `product_plan.md` Никиты переписан с градацией по пользе + сдвигом к автономному агенту. (3) **Архитектор v2 прогнан интерактивно на Никите (2026-06-01):** реальный ресёрч рынка (Mira/Poke/VoiceNote = коммодити; Hermes ≈ LangGraph-класс; session-память коробок = наш ров), предметный «день Никиты», подтвердил архитектуру + вскрыл, что ров = сквозная память по проектам, не проактивность. Финализированы `product_plan.md` + `product_spec_brief.md` с согласованными решениями Алика (память в MVP, docx, ведущий угол «система+проекты+память»). **ДЫРА 2 закрыта.**
**Побочно — самокоррекция (см. failures.md 2026-06-01):** цена Opus = **$5/$25** (текущий 4.5+), а не $15/$75 — архитектор был прав оба раза, я ошибочно передавил верное число устаревшей памятью при sanity-check. Урок перевёрнут.
Потом (отложено): технический лендинг для Алика (визуальный, один раз, через дизайнера+фронтендера) — НЕ сейчас.

### Версионирование лендингов [отложено, после оценщика]
Реестр на клиента: `clients/<slug>/_sites_registry.md` (№, дата, концепция, угол продажи, URL, статус live/offline).
Код каждой версии сохраняется (`deliverables/<slug>/v1-.../`, `v2-.../`) даже если деплой отключён — чтобы передеплоить.
Операции (skill `manage_sites`): **список** / **отключить** (удалить деплой, строка в реестре остаётся = архив) / **включить** (передеплой) / **новый вариант**.
Варианты: и разные ТЕКСТЫ (углы продажи), и разные ДИЗАЙНЫ — несколько за раз, выбирать лучший.

## Приватность персональных КП

Лендинг под клиента — это **персональное коммерческое предложение**, не публичный сайт. Политика приватности:

1. **noindex, nofollow в мете** — `<meta name="robots" content="noindex, nofollow">` + `robots.txt` с `Disallow: /`. Не в поисковики. Бесплатно, **ВСЕГДА** для персональных КП.
2. **Обезличенный URL** — без имени клиента (`arhiv-sistemy`, не `nikita-...`).
3. **Внутри — ПОЛНАЯ персонализация.** Имя клиента (в тексте), реальные проекты, боли его словами — это СИЛА КП, использовать НУЖНО. Раз стоит noindex + URL не палевный — посторонний страницу не найдёт.
4. **Password protection (Vercel)** — опция для чувствительных, ПЛАТНО. Пока не используем (см. backlog).

Запрет НЕ на данные клиента (их использовать нужно — это и есть ценность КП), а на три вещи:
- **публичную индексацию** → noindex + robots.txt;
- **имя клиента в URL** → обезличить домен;
- **выдуманные факты** → урок «Альфа Холдинга» (отдельная дисциплина, действует всегда).

**Нюанс по имени в мете/хедере:** реальные ПРОЕКТЫ клиента в контенте/мокапах = узнавание (хорошо). Личное ИМЯ клиента в `<head>` (author/og) и в хедере-бейдже = чистое палево без пользы для конверсии → НЕ выносить даже при noindex. Имя живёт в видимом тексте КП, где работает на узнавание, а не в служебных мета.

## Backlog (вдруг понадобится)
- capabilities.md — описание доступных инструментов/сервисов/среды. Убрано из плана (архитектор не ограничен навыками/средой, Claude Code может многое). Добавить если оценки архитектора окажутся слишком абстрактными без знания конкретной среды.
- Password-protection лендинга (Vercel, платно) — если понадобится жёстче, чем noindex + обезличенный URL (например, лендинг с чувствительными цифрами/коммерческой тайной клиента).
- Системное усиление промптов (после выбора лучшего варианта Никиты): зашить в дизайнера «всегда показывай результат предметно (мокапы/схемы/до-после)», в копирайтера «предметный показ результата + источник доверия об авторе». Чтобы дыра «суховато / не видно результат» не повторялась на новых клиентах. Триггер: после того как на Никите определим, что работает.

## Открытые вопросы / хвосты
- **СКВОЗНАЯ ПАМЯТЬ (ров Никиты) — НЕ построена, только роадмап.** `product_plan.md` подаёт её как главный ров и часть MVP, но в железе её нет: бот сейчас на контекстном окне / последних ~10 сообщениях, сессия сбрасывается, домены не сшиваются. **Не подавать как готовое — ни в копи, ни на созвоне (только как вижн/траектория).** Построить отдельный слой: хранилище + извлечение, переживает сессии, сшивает проекты. Прототип идёт в **Рей Вектор** → паттерн в клиентский продукт. Подробно — `../../BACKLOG.md` (2026-06-02, #moat #memory). Урок-параллель: мина «Джарвис читает чаты» коуча (`benchmark_coach_vs_ours.md`).
- Клиентские данные — единственная копия в git? Бэкап на VPS 62yun — в backlog
- orphan-проект Vercel `original` (original-theta.vercel.app) — удалить: vercel project rm original
- Никита — пилот офиса (его лендинг переделываем ЧЕРЕЗ офис, не вручную)
- Конвенция имён артефактов — зафиксирована отдельно: `naming-convention.md` (strategist_brief.md, product_spec_brief.md, product_plan.md, copywriter_draft.md, design_spec.md, deliverables/<slug>/, _agent_notes.md, _director_report.md)

## Уроки / выводы
- **Потолок продающей силы = зрелость продукта, не качество копи** — `lessons-quality-ceiling.md`. Честный потолок копи ≈65/80, дальше двигает первый кейс + демо, не текст. Не зацикливать итерации лендинга. Честность выдержала стресс-тест «дай максимум» (reward-hacking не случился).
- **Бенчмарк против лендинга коуча** — `benchmark_coach_vs_ours.md`. Паритет по продаже (91 vs 90), мы честнее (architect-проверка vs мины коуча).

## История исследований
- Детальные разборы оркестрирующих систем: orchestration-research.md
