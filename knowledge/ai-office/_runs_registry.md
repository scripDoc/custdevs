# _runs_registry — реестр прогонов лендинг-пайплайна

Каждая строка — один прогон Директора (или эквивалентная сборка лендинга) по клиенту. Источник пометок «уже прогнан» для пикера Директора. Дописывается в КОНЕЦ после каждого прогона (история не затирается). Деплои отдельно — в `_sites_registry.md`.

**Вердикт:** `PASS` / `BLOCK` (по qa-controller) / `частично` (остановились на стадии) / `—` (не дошли до qa).
**Превью:** preview-URL (НЕ прод). Прод-деплой — отдельным шагом по «го» Алика, фиксируется в `_sites_registry.md`.

| Клиент (slug) | RUN ID | Дата | Вердикт qa | Превью URL | Эскалации | Заметка |
|---|---|---|---|---|---|---|
| nikita_muzh_klub | nikita-manual-honest-v6 | 2026-06-05 | PASS | **прод: https://arhiv-sistemy.vercel.app/** (старый URL, деплой 2026-06-06) | 1 | Ручной honest-фикс v6 (не через Директора): память=траектория, фазы секвенированы, конфляция «8» убрана. **Решение Алика 2026-06-06: честный контент на СТАРЫЙ URL** — задеплоен `--prod` в существующий проект `arhiv-sistemy` (не новый), URL прежний. v5 откатим (`deliverables/nikita_muzh_klub_v5_frozen/`). См. `_qa_report_v6.md`, `_sites_registry.md`. |
| sergey_psiholog | sergey_psiholog-20260606-0441 | 2026-06-06 | PASS (G1+G2) | https://tsifrovoy-sarafan-252oafz07-scripdocs-projects.vercel.app (превью, за auth-валлом 401) | 3 (все низк., не блок.) | **ПЕРВЫЙ реальный автономный прогон через Директора** (полная цепочка, не ручной фикс). Оба гейта PASS, 0 блокеров, re-check не нужен. Оффер «Поток без энергии» / механизм «Цифровой сарафан»; поток честно = Фаза 2. Превью только, prod не трогали. Эскалации: контентный гэп (нет примера «в его голосе» — sales), CTA-плейсхолдер, auth-валл превью. См. `clients/sergey_psiholog/_director_report.md`, `_qa_report.md`. |

## История / заметки

- **2026-06-05** — реестр заведён. Первая запись — пилот `nikita_muzh_klub` (честная v6), собран вне Директора (ручной фикс по цепочке spec_brief→копи), qa PASS, превью ещё не деплоен. Остальные клиенты (`aleksandr_klub_avto_optika`, `aleksei_3d_print`, `sergey_psiholog`, `vladimir`) прогонов ещё не имели — в пикере пойдут как «новый».
- **2026-06-06** — пилот `nikita_muzh_klub` доведён до прода: честный v6 задеплоен на СТАРЫЙ URL `https://arhiv-sistemy.vercel.app/` (в существующий проект, без новой ссылки). Прод-URL проставлен в строку выше.
- **2026-06-06** — **первый реальный автономный прогон Директора** по `sergey_psiholog` (нейтральный slug деплоя `tsifrovoy-sarafan`). Полная цепочка стратег→архитектор→копи→G1 qa→sales→дизайн→фронт→G2 qa отработала end-to-end; оба гейта честности PASS, 0 блокеров. Зашитое сработало: qa-первым-гейтом, чистый-handoff, честность-по-дефолту (память=траектория, фазы секвенированы, факты грунтованы), деплой только превью без `--prod`. Превью за Vercel Deployment Protection (401) — публичная отдача = отдельный шаг по «го».
